NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Schwab (infrastructure — user credentials are stored per-user in the database)
SCHWAB_CALLBACK_URL=https://your-domain.com/schwab/callback  # Optional: auto-detected from request if not set
SCHWAB_TOKEN_ENCRYPTION_KEY=generate-with-openssl-rand-hex-32
